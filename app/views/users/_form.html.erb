<%= form_for @user do |f| %>
    <div>
        <%= f.label :username %> <br/>
        <%= f.text_field :username %>
    </div>
    <div>
        <%= f.label :email %><br />
        <%= f.text_field :email %>
    </div>
    <div>
        <%= f.label :password %><br />
        <%= f.password_field :password %>
    </div>
    <%= f.submit(@user.new_record? ? "Sign Up" : "Edit User", class: "btn btn-primary mt-3") %>
<% end %>

<%= link_to 'Back to show all Articles', articles_path%>