<p>Showing Selected Articles</p>
<div class="card p-2 my-3">
    <h1><%= @article.title%></h1>
    <p>Created By: <b><%= (@article.user.username) if @article.user %></b></p>
    <p><b>Description :</b> <%= @article.description%></p>

    <div class="mt-2">
        <% if logged_in? && current_user == @article.user %>
            <%= link_to 'Edit this Article', edit_article_path(@article), class: "btn btn-warning btn-sm" %>
        <% end %>
        <%= link_to 'Show all Articles', articles_path, class: "btn btn-primary btn-sm"%> 
    </div>
</div>

